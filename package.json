{
  "homepage": "https://robertsonheather.com",
  "dependencies": {
    "@gsap/react": "^2.1.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -b main -d build"
  }
}
